using System;
using UnityEngine;

public class MessageIteractAction : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    MessageController message;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void Action() 
    {

    }
}
